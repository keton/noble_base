{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${file}",
            "outFiles": [
                "${workspaceRoot}/lib/**/*.js",
                "${workspaceRoot}/lib/**/*.js.map",
                "${workspaceRoot}/lib/**/*.d.ts"
            ]
        }
    ]
}